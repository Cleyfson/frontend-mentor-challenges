import React from "react";

const Btn = () => {
	return (
		<div className="buttons">
			<button className="buttons__btn btn--one">Email</button>
			<button className="buttons__btn btn--two">Linkedin</button>
		</div>
	);
};

export default Btn;
